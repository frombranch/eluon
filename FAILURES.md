# 실패 기록

여기 있는 것은 전부 **실제로 일어난 일**입니다. 규칙서(`CLAUDE.md`)의 조항이 왜 그렇게 쓰였는지
설명이 필요할 때만 봅니다. 규칙 본문에는 한 줄과 이 파일의 앵커만 둡니다 —
사례를 본문에 두면 규칙서가 계속 길어지고, 길어지면 안 읽힙니다.

새 사례는 **아래에 이어서** 씁니다. 기존 항목을 고치지 않습니다.
형식은 셋입니다 — **무슨 일이 있었나 / 왜 못 막았나 / 지금은 어느 조항이 막나.**

---

## 관문을 건너뛴 것

### `#gate-skipped` — 호텔 사이트인데 예약 자산이 하나도 없었다

고객사 테마를 뜰 때 **값(색·치수)만 재고 자산 대조를 하지 않았다.**
그 결과 호텔 사이트인데 예약 부품이 라이브러리에 하나도 없다는 것을 모른 채,
`form-field-md`+`select-md` 로 일반 폼을 만들어 예약 검색 자리를 메꿨다.

**왜 못 막았나.** 화면이 멀쩡히 나왔다. 자산 규격도 실측 토큰도 전부 맞았다.
QA 표에 한 줄도 걸리지 않았다. 고객이 "호텔에서 가장 중요한 예약이 빠졌다"고
지적하고서야 발견됐다. **"일단 되니까 넘어간다"가 가장 위험한 신호다.**

**구조적 원인.** 자산 목록이 테마와 무관하게 고정이라 "이 고객사에 무엇이 더 필요한가"를
물을 자리가 자체가 없었다. 지금은 §A-G2 와 §D 의 `assets.required` 가 그 자리다.

### `#brief-order` — 브리프 나열 순서를 배치 순서로 읽었다

`GNB / 검색 / 히어로 / …` 라고 적힌 섹션 목록을 그대로 따라 검색 폼을 히어로 위에 놓았다.
나열은 "이런 것들이 들어간다"는 뜻이지 "이 순서로 쌓아라"가 아니다.

**지금 막는 조항** — §G 「페이지의 뼈대 순서」.

### `#substitute-blind` — 조합해서 만들면 "없다"는 자각이 사라진다

§0-2 에 "manifest 에 없는 컴포넌트가 필요하면 보고한다"가 **있었다.**
그런데 두 자산을 겹쳐 "비슷하게" 만드는 순간 필요를 느끼지 않으므로 조항이 발화하지 않았다.

**교훈.** 에이전트의 자각을 조건으로 삼는 규칙은 자각 실패 시 무력하다.
트리거를 관찰 가능한 사건(표에 오르는 것)으로 바꿔야 한다.

---

## 제안이 없었던 것

### `#no-proposal` — 규격은 다 맞았는데 인상이 없었다

자산 규격과 실측 토큰을 전부 맞춘 시안에서 고객 반응이 **"딱딱하고 옛스럽다, 느낌이 없다"** 였다.
측정해 보니 — 섹션 일곱 개가 **전부 같은 틀**(제목 → 리드 → 그리드), 구성 형태 1종·점유율 100%.
받은 사진 **26장 중 1장만** 크게 썼다(사용률 4%). 활자는 큰 제목 하나 말고 크기가 다 비슷했다.

**왜 못 막았나.** 지켜진 규칙은 전부 **코드에 흔적이 남고 grep 으로 셀 수 있는 것**이었다
(색·수치·시간·이미지 비율). 무시된 규칙은 전부 **HTML 에 흔적이 안 남는 대화 산출물**이었다
(자산 대조표·제안·보고). **준수율은 중요도가 아니라 검증 가능성에 비례했다.**

**지금 막는 조항** — §B 점검표(형태 3종·사진 사용률·활자 대비)와 §A-G3 의 ③ 3건 하한.

### `#rhythm-overread` — 「여백을 통일하라」를 「구성도 통일하라」로 읽었다

§7-4 의 *"리듬은 두 값뿐 … 섹션마다 여백을 다르게 주지 않는다"* 를 확대 해석했다.
본문은 **여백 수치**만 다루는데 제목의 명사(「리듬」)가 본문보다 넓었다.

**교훈.** 제목의 명사가 본문 범위보다 넓으면 그 차이만큼 확대 해석된다.
금지 문장에는 **짝이 되는 요구 문장**을 같은 자리에 붙인다.

### `#photo-buried` — 가장 안 팔리는 컷을 앞에 뒀다

호텔 지점 카드 5장을 **전부 건물 외관**으로 채웠다(한 피사체 100%).
객실·조식·수영장·라운지 사진은 탭 안쪽에 있었고 첫 화면에서 한 장도 보이지 않았다.
사람이 호텔을 고를 때 보는 것은 **자기가 묵을 방**이지 로비 밖에서 찍은 건물이 아니다.

**교훈.** **탭 안쪽은 없는 것과 같다.**

---

## 테마·토큰

### `#rem-trap` — 환산했더니 테두리 1px 이 1.6 이 됐다

`harbor` 에서 뷰포트 비례(fluid rem) 배치인 사이트를 기준 폭으로 환산하다가,
**rem 인 값과 고정 px 인 값을 구분하지 않았다.** 테두리는 보통 고정 px 이라 환산하면 안 된다.

**지금 막는 조항** — §D. 루트 폰트 크기를 두 배로 올려 다시 재고, 움직인 값만 rem 이다.

### `#core-polluted` — 업종 어휘를 core 에 넣어 전 테마를 오염시켰다

예약 치수(`booking.*` 11키)와 툴팁 치수(`tooltip.*` 4키)를 `tokens/core.json` 에 넣었다.
`build_measure.py` 가 core 의 `semantic.size` 앞머리로 축을 세므로 **치수 축이 27 → 29** 가 됐고,
**예약이 없는 커머스·금융 고객사에게도 "예약 여백을 아직 안 쟀다"고 요구하는 `MEASURE.md`** 가 만들어졌다.

**교훈.** core 는 "공통으로 쓰이는 것"이 아니라 **"업종이 바뀌어도 참인 것"** 이다.
둘을 혼동하면 업종 어휘가 core 로 새어 들어간다.

### `#font-leak` — 브랜드 전용 서체 이름으로 고객사가 드러났다

고객사 테마 파일에 그 회사의 **전용 브랜드 서체 이름**을 적었고, 그대로 public 저장소에 커밋됐다.
테마 id 는 가명이었지만 서체 이름 하나로 어느 회사인지 특정된다.

**지금 막는 조항** — §I. 서체가 다르다는 사실은 적되 **이름은 적지 않는다.**

### `#demo-data-leak` — 레시피의 데모 데이터도 공개물이다

`recipes/components.py` 의 표 데모 행에 실제 프로젝트명이 들어 있었고,
그것이 Pages·PNG·엘루온 시트·jsDelivr 에 전부 구워져 public 에 노출됐다.

**지금 막는 조항** — §I. 데모 데이터에 실제 프로젝트명·사람 이름을 쓰지 않는다.

### `#doc-token-drift` — 문서의 숫자와 토큰이 어긋났다

§6 이 `--motion-entrance` 를 **420ms**, `--motion-stagger` 를 **60ms** 라고 적고 있었는데
`core.json` 의 실제 값은 **640 · 80** 이었다. "축약형 다섯"도 실제로는 여섯이었다.

**교훈.** 문서에 숫자를 적으면 토큰과 갈라진다. **토큰이 정답**이라고 쓰고 숫자는 적지 않는다.

### `#missing-var` — 존재하지 않는 CSS 변수는 조용히 빈 값이 된다

`var(--size-header-actionHeight)` 를 썼는데 그런 변수가 없어 헤더 버튼이
48px 대신 **21.75px** 로 찌그러졌다. 에러가 나지 않아 눈으로 봐야만 안다.

**지금 막는 조항** — §C. `specByTheme` 에서 실제 값을 읽고 **같은 값을 가진 semantic 토큰**을 찾아 쓴다.

### `#stale-clone` — 낡은 복제본을 보고 자산을 없다고 판정했다

v1.0.1 에 멈춘 복제본의 `index/ASSETS.md` 가 22개만 보여줘서,
그 뒤 추가된 커머스 5종과 `section-header-md` 를 "없는 자산"으로 잘못 분류했다.

**교훈.** 자산을 조회할 때는 언제나 정본 저장소를 본다.

---

## 접근성·대비

### `#contrast-measured` — 실측 색이 접근성 기준에 걸린다

여러 고객사 테마에서 실측한 회색이 기준에 못 미쳤다.
`#999999`(2.85:1) · `#B5B2B0`(2.11:1) 은 본문(4.5)뿐 아니라 UI 경계(3.0)도 못 넘긴다.
`#CCCCCC`(1.61:1) 도 경계로 쓸 수 없다.

**지금 하는 것** — 기준 미달값은 `text.disabled` 처럼 **기준이 없는 자리로 내리고**,
필요한 자리에는 최소 통과값을 파생해 넣는다. 그리고 `$derived` 에 무엇을 왜 바꿨는지 적는다.

### `#accent-unreadable` — 브랜드 accent 를 글자색으로 쓸 수 없다

ELUO 의 accent `#FFFF01` 은 흰 배경 위 **1.07:1** 로 전혀 읽히지 않는다.
`atlas` 의 원본 코랄 `#D85640` 도 흰 텍스트 3.94 · 짙은 텍스트 4.38 로 양쪽 다 AA 미달이었다.

**지금 하는 것** — 그런 색은 **면 강조에만** 쓰고 텍스트 색으로 쓰지 않는다.

### `#title-clipped` — 한글 제목에 nowrap 을 걸어 통째로 잘렸다

한글 공지 제목에 `white-space:nowrap` 과 `max-width:44ch` 가 걸려 제목이 통째로 잘렸다.
`44ch`·`52ch` 는 어느 spec 에도 없는 값이었다.

**지금 막는 조항** — §E. 한글 제목은 두 줄까지 허용하고, spec 에 없는 `ch` 숫자를 만들지 않는다.

---

## 반응형

### `#tablet-abandoned` — 768–1023 을 손대지 않고 넘어갔다

브레이크포인트가 767·479 둘뿐인 시안이 나왔다. **1023px 에서 4열 카드의 폭이 225px 로 줄어**
카드 발치의 배지와 버튼이 줄바꿈되며 무너졌다. **고객은 제안서를 아이패드로 연다.**

**지금 막는 조항** — §B 점검표. 768–1023 구간을 다루는 규칙이 1개 이상 있어야 한다.

### `#hidden-attr` — 작성자 `display` 선언이 `[hidden]` 을 이긴다

`.btn{display:inline-flex}` 같은 선언이 브라우저 기본 `[hidden]{display:none}` 을 항상 이겨서,
필터·빈 상태·토글 버튼이 **하나도 감춰지지 않았다.** `el.hidden` 속성만 읽는 검사는 통과했다.

**교훈.** 감춤은 **속성이 아니라 실제 렌더 높이로** 확인한다. 그리고 `[hidden]{display:none!important}` 를 base 에 둔다.

---

## 모션

### `#motion-900ms` — 크로스페이드가 주기의 18% 를 먹었다

5초마다 도는 히어로의 크로스페이드가 **900ms** 여서 매 주기의 18% 가 디졸브였다.

### `#autoplay-mouseenter` — 터치에서는 멈출 방법이 없었다

히어로 슬라이드가 `mouseenter` 로만 멈추는 시안이 나왔다.
터치 기기에는 hover 가 없어 페이지에서 **가장 큰 요소가 WCAG 2.2.2 위반**이었다.

**지금 막는 조항** — §F. 자동 재생은 멈춤 버튼 · reduced-motion · `document.hidden` 셋을 다 갖춰야 한다.

### `#hover-on-article` — 아무 데도 안 가는 카드가 반응했다

링크가 없는 `<article>` 카드에 hover 확대가 걸렸다.
iOS 의 sticky `:hover` 때문에 **탭한 뒤 확대된 채로 남았다.**

**지금 막는 조항** — §E·§F. hover 는 링크·버튼일 때만. 나란한 같은 종류 카드는 전부 링크이거나 전부 아니어야 한다.

### `#static-screen` — 규격을 다 지키고도 정적인 화면이 나왔다

자산 규격을 전부 지켰는데 **칩 0개 · 탭 0개 · 모달 0개**인 시안이 나왔다.
모션 규칙이 아니라 화면 구성이 문제였다.

**지금 막는 조항** — §B 점검표. 누를 수 있는 것 합계 ≥ 1.

---

## 이미지

### `#blurry-2x` — 700px 이미지를 2배 밀도 화면에 띄웠다

표시 폭의 2배로 요청하지 않아 사진이 흐렸다. **흐린 사진이 시안을 가장 빨리 싸게 만든다.**

**지금 막는 조항** — §F. 표시 폭 × `--media-density`.

---

## 빌드·파이프라인

### `#group-enum` — 그룹 이름이 다섯 곳에 흩어져 있다

`v1.10.0` 에서 `disclosure` 를 `schema/asset.schema.json` 의 enum 에 안 넣어
**푸시 4건이 연달아 실패**했다. 그룹을 추가하면 고쳐야 할 곳이 다섯 곳이다 —
`render.GROUP_DIR` · `build_docs.GROUP_KO` · `GROUP_EN` · `build_pb.GROUP_KO` · 스키마 enum.

### `#version-bump` — 버전을 올리면 수기 문서도 같이 고쳐야 한다

파이프라인이 안 건드리는 곳이 넷이다 — `README.md` · `CLAUDE.md` · `prompts/01` · `prompts/03`.
빌드가 이것을 검사해 막는다. `manifest.json` 의 `since` 는 자산 도입 시점이라 **올리면 안 된다.**

### `#sidecar-first-theme` — 첫 테마가 그 자산을 안 들면 사이드카가 안 써진다

`render.py` 가 사이드카를 `if theme == themes[0]` 일 때만 쓴다.
자산이 테마별로 갈리는 구조로 가면 이 조건이 깨진다. 테마 루프 밖으로 빼야 한다.
*(아직 일어나지 않았다. 3층 구조를 배선할 때 같이 고친다.)*
