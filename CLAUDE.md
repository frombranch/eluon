# ELUON — 에이전트 작업 계약서

Eluon(엘루온)은 디자인포지션의 디자인 시스템입니다.
이 파일은 사람이 아니라 **AI 에이전트가 먼저 읽는 규칙서**입니다.

**읽는 순서가 하는 순서입니다.** §A 관문을 순서대로 통과하고, §B 점검표로 끝냅니다.
나머지 절(§C~§I)은 그때그때 찾아보는 참조입니다.

실패 사례는 이 파일에 없습니다 — `FAILURES.md` 에 있고, 본문에는 앵커만 둡니다.

---

## A. 관문 — 순서대로 통과한다

일은 **두 트랙**입니다. 섞지 않습니다.

| 트랙 | 언제 | 산출물 | 횟수 |
|---|---|---|---|
| **T1 · 사이트 온보딩** | 그 고객사를 처음 맡을 때 | **사이트 정의**(핵심 컴포넌트 인벤토리 + 테마 + 강약점) | **1회** |
| **T2 · 페이지 요청** | 새 페이지를 만들어 달라고 할 때 | 시안 + **개선 제안** | 매번 |

**T1 을 건너뛰고 T2 로 갈 수 없습니다.** 사이트 정의가 없으면 T1 부터 합니다.
**T1 을 다시 돌리는 것은 사이트가 개편됐을 때뿐입니다** — 페이지 요청마다 다시 훑지 않습니다.

우리는 **운영을 맡은 대행사**입니다. 그래서 T2 에서 요청받은 것만 만들면 부족합니다.
**기존 사이트의 핵심을 이미 알고 있고, 그 위에 더 나은 방향을 얹는 것**이 UX 컨설팅의 기본 자세입니다.
T1 이 "이미 알고 있다"를 만들고, T2 의 G3 가 "더 나은 방향"을 만듭니다.

**통과의 증거는 산출물입니다.** "읽었다"는 통과가 아니고, 표나 목록을 내야 통과입니다.
결과물 하단에 관문 통과표를 남깁니다. 흔적이 남지 않는 규칙은 지켜지지 않습니다(`FAILURES.md#no-proposal`).

### T1 — 사이트 온보딩 (1회)

전제: **사이트는 이미 열려 있고 우리는 그 운영을 맡는다.** 새로 짓는 게 아니다.
시작 입력은 브리프가 아니라 **URL**이다.

| 단계 | 무엇을 | 통과 조건 |
|---|---|---|
| **T1-1 전 페이지 훑기** | `sitemap.xml`(없으면 내부 링크) → **URL 패턴별 개수**와 페이지 유형 목록 | 3회 이상 반복되는 템플릿이 잡혔다 |
| **T1-2 생계 판정** | "이 사이트는 **○○**로 먹고산다" + **근거 신호 3개** | 신호 셋 이상이 한곳을 가리킨다 |
| **T1-3 흐름 해부** | 그 흐름을 **결제 직전까지 실제로 밟는다** | 확인 화면까지 도달했다 |
| **T1-4 컴포넌트 인벤토리** | **모든 페이지 유형**에서 반복되는 조각 + 반복 횟수 → manifest 대조 | 표 둘(있는 것 / 없는 것)이 나왔다 |
| **T1-5 값 실측** | computed style — 색·활자·치수·라운드·컨테이너 | rem 과 고정 px 를 구분했다(§D) |
| **T1-6 강약점** | ①빠짐없이 ②그대로 ③바꿀 것 표 셋 | **사람의 승인** |
| **T1-7 저장** | 사이트 정의 — 인벤토리 · 테마 · `coreFlow` · `requiredAssets` · 개선 목록 | 다음 요청 때 **다시 훑지 않아도 되는 상태** |

**값(T1-5)보다 구조(T1-2·3·4)가 먼저다.** 색·치수부터 재면 화면은 나오고 규격도 맞아서
인벤토리가 통째로 사라진다(`FAILURES.md#gate-skipped`).

⚠️ **인벤토리는 이 페이지가 아니라 사이트 전체를 대상으로 한다.** 지금 만들 페이지에만
필요한 것을 세면, 다음 요청 때 또 훑게 되고 그때마다 빠지는 것이 생긴다.

### T1-2 ~ T1-4 를 어떻게 하나

**생계 판정** — 추측은 신호가 아니다. **화면에서 센 것만 신호다.**

**대조** — 부품 목록 × `manifest.json`. **반복 횟수를 함께 적는다.**

   | 있는 것 | 자산ID | 반복 횟수 | 핵심 흐름 부품? |
   | 없는 것 | 이름 | 반복 횟수 | 핵심 흐름 부품? | 대체 후보 |

   **두 표 다 0 이면 대조를 안 한 것이다.** 비어 있어도 표는 낸다.

**생계를 세는 신호** — 전역 헤더의 **채움형 버튼**(보통 하나, 그 목적지가 생계) · 첫 화면 폼의
**입력 항목**(지점·날짜·인원 = 예약) · **외부 도메인 링크**(예약엔진·PG·ATS 로 나가면 그 기능은 외부) ·
URL 패턴 빈도 · JSON-LD `@type` · 375 에서 남는 **하단 고정 바** · 푸터의 **사후 조회 링크**
("예약 조회"는 그 흐름이 실제로 돈다는 증거) · `form action`·GA 이벤트명.

**업종별 「빠지면 안 되는 핵심 흐름」표는 `docs/REFERENCE.md` 에 있다.**
업종을 지목했으면 그 줄을 펼쳐 부품 목록을 얻고, **그 부품이 대조 1순위다.**
표에 없는 업종이면 신호로 흐름을 직접 적고, 표에 한 줄 추가할지 사람에게 제안한다.

**멈춤 조건** — 이때는 그리지 말고 보고한다.

- 흐름 부품 중 하나라도 manifest 에 없을 때. **두 자산을 겹쳐 "비슷하게" 만드는 것은 대체가 아니라 신규 제작이다**(`FAILURES.md#substitute-blind`)
- 생계 신호가 셋 미만이거나 서로 다른 곳을 가리킬 때
- 흐름이 로그인·결제 뒤에 있어 끝까지 못 밟을 때 — 관찰한 데까지만 내고 나머지를 요청한다. 추정으로 채우지 않는다
- 흐름이 외부 도메인으로 나갈 때 — 계약 범위를 사람이 확인해야 한다
- URL 이 없거나 접속이 막힐 때

멈출 때 내는 것은 셋 — **지금까지의 표 · 막힌 지점 한 문장 · 선택지 둘.**
**"일단 비슷한 것으로 채워두겠습니다"는 금지한다.**

> 사람이 "일단 진행"이라고 답하면, 없는 것 목록을 결과물 하단에 그대로 싣고
> 대체 자산에 `<!-- SUBSTITUTE: 예약위젯 → form-field-md+select-md (임시) -->` 주석을 단다.
> **대체는 허용하되 흔적을 남기는 것이 조건이다.**

### T2 — 페이지 요청 (매번)

| 관문 | 내야 통과 | 통과 못 하면 |
|---|---|---|
| **G0 · 무슨 일인지** | 4줄 카드 — `고객사/자사` · `사이트 정의 있음/없음` · `요청받은 페이지` · `산출물 종류` | **사이트 정의가 없으면 T1 부터 한다.** 고객사 건인데 URL 도 없으면 물어본다 |
| **G1 · 테마** | 사이트 정의에서 `테마 id` · `$status` · **실측 안 된 축 목록**을 읽어 낸다 | 모르면 물어본다. `estimated` 는 대외 제출물에 쓰지 않는다 |
| **G2 · 이 페이지에 필요한 것** | 사이트 인벤토리에서 이 페이지가 쓸 부품을 골라 낸다. **인벤토리에 없는 것이 필요하면 적는다** | 인벤토리에 없고 manifest 에도 없으면 **T1-4 로 되돌아가 인벤토리를 갱신하고 보고한다** |
| **G3 · ②/③ 분리** | 축별 3열 표 — `그대로(②)` / `바꿀 것(③)` / `근거` | **③이 3건 미만이면 통과가 아니다.** 복사기 상태다 |
| **G4 · 구성 계획** | 섹션 표 — `순서 / 섹션명 / 자산ID / 형태 / 폭 / 배분 / 정렬 / 앞 섹션과 왜 다른가` | 형태 3종 미만이거나 자산 ID 가 manifest 에 없으면 되돌아간다. **여기까지 코드를 쓰지 않는다** |
| **G5 · 조립** | HTML + 사용 자산 ID 목록 + 관문 통과표 + 범용 이미지 출처 한 줄 | — |
| **G6 · 제출 전 점검** | §B 전 항목 `통과/실패/해당없음` 표 | **실패가 하나라도 있으면 제출하지 않는다** |

### G3 — ②/③ 분리 (T1-6 에서 만들고, T2 에서 매번 갱신한다)

우리는 대행사입니다. 역할이 둘이고 **둘 다 해야 합니다** — 빠짐없이 아는 것과, **더 나은 것을 제안하는 것.**

| 갈래 | 무엇 | 다음 행동 |
|---|---|---|
| ① 반드시 있어야 하는 것 | 핵심 흐름 부품 | 없으면 G2 에서 멈춤 |
| ② 그대로 가져갈 것 | 브랜드 인상을 만드는 값 — 색·활자·라운드 | 테마로 실측해 옮긴다 |
| ③ 바꾸자고 제안할 것 | 약점 — 동선·위계·반응형·접근성·밀도·이미지 | **근거와 함께 제안한다** |

**③이 비면 우리는 복사기입니다.** 요청받은 것만 만들면 대행이지 컨설팅이 아니다.
T1 에서 만든 ③ 목록을 **T2 마다 이 페이지 기준으로 다시 계산한다** — 사이트 전체의 약점이 아니라
**지금 만드는 페이지에서 무엇을 더 낫게 할 수 있는지**를 낸다.
리뉴얼 건에서 현재 사이트는 **결과물이자 동시에 바꿀 대상**이다.
실측한 축을 이 표에서 갈라 적기 전에는 **어느 값도 기준이 아니다.**
기본 배분 — 색·활자·라운드는 ②, 간격·동선·위계·반응형·접근성은 **③ 후보**로 놓고 시작한다.

**약점은 판정선으로 찾는다.** 재는 것과 판정선이 같이 있어야 측정이다.
판정선이 없으면 "쟀습니다"가 통과 도장이 된다.

| 축 | 이 수치를 넘으면 약점이다 |
|---|---|
| 접근성 | 본문 대비 4.5 미만 · 큰 활자 3.0 미만 · 히트 44px 미만 · 포커스 링 없음 · `alt` 누락 — **전부 0이어야 통과** |
| 반응형 | 가로 넘침 1건 이상 · **768–1023 전용 분기 0개** · 카드 최소 폭 260px 미만 |
| 동선 | 핵심 업무 입구까지 클릭 3회 이상 · 375 첫 화면에서 입구가 안 보임 |
| 정보 위계 | `h2` 만 이어 읽어 요지 복원 실패 · `h2` 9개 이상 · 뜻이 겹치는 `h2` 2개 이상 |
| 밀도·리듬 | **형태 3종 미만** · 최다 형태 점유율 50% 초과 · 같은 형태 3회 연속 |
| 활자 | 크기 단계 4개 미만 · **최대 ÷ 본문 3.0 미만** · 최대 점프 1.6배 미만 |
| 이미지 | **사용률 60% 미만** · 최대 사진이 콘텐츠 폭의 60% 미만 · 면적비 25% 미만 |

**제안은 절차로 만든다.** ① 판정선을 넘긴 축을 격차 큰 순으로 → ② **상위 3개까지만**(4개째부터는
개편이지 제안이 아니다) → ③ 축 하나에 개선안 하나(둘을 건드리면 쪼갠다) → ④ **바뀐 뒤 수치를 계산해 적는다**
→ ⑤ ②를 침범하는지 스스로 선언 → ⑥ 섹션 단위로 잘라 낸다(한 건이 거절돼도 나머지가 살게).

근거는 **측정 · 규칙 조항 · 업종 관측(몇 개 중 몇 개인지)** 셋 중 둘 이상.
**형용사만 있는 제안은 제출하지 않는다.** 문장에 숫자가 없으면 그 줄을 지운다.
각 제안에 **"안 받으면 남는 약점"** 을 한 줄로 적는다 — 고객은 취향이 아니라 **감수할 약점**을 고른다.

---

## B. 제출 전 점검표

전부 세거나 grep 하거나 브라우저로 확인 가능한 항목입니다. "좋은가"는 없습니다.

| # | 항목 | # | 항목 |
|---|---|---|---|
| 1 | 관문 통과표 7줄이 하단에 있다 | 14 | 375·768·1024 가로 스크롤 **0** |
| 2 | 사용 자산 ID 가 전부 manifest 에 있다 | 15 | `outline:none` 0 · `:focus-visible` 1개 이상 |
| 3 | 자산을 겹쳐 새 부품처럼 쓴 곳 **0** (있으면 `SUBSTITUTE` 주석) | 16 | 본문 `<br>` 0 · `keep-all` 과 `overflow-wrap:anywhere` 가 **짝**으로 |
| 4 | 생계 한 문장이 결과 상단에 있다 | 17 | 형태 **3종↑** · 폭 **2단계↑** · 같은 배분 연속 3회 **0** |
| 5 | 헥스 색 리터럴 **0** (data URI 제외) | 18 | 누를 수 있는 것(탭·칩·아코디언·모달) **≥1** |
| 6 | `font-size:` 뒤 숫자 리터럴 **0** | 19 | hover 걸린 것 중 `<a>`·`<button>` 아닌 것 **0** |
| 7 | 시간 리터럴 **0** | 20 | 사진 위 글자에 요소 `opacity` **0** · 스크림 있음 |
| 8 | `var(--space-*)` 밖 여백 리터럴 **0** (0·1px 제외) | 21 | 목록·표·검색이 있으면 빈 상태 **1개** |
| 9 | `display1` 1개 · `cta-band-lg` 1개 | 22 | `alt` 없는 `<img>` **0** |
| 10 | 아이브로우 **≤2** | 23 | 이미지 요청 폭 ÷ 표시 폭 최소값 **≥2.0** |
| 11 | `--media-ratio-*` 밖 `aspect-ratio` **0** | 24 | 전폭 또는 큰 크기 사진 **≥1장** |
| 12 | `min-width` 미디어쿼리 **0** | 25 | G3 ③ 항목 **≥3**, 각각 측정 근거 한 줄 |
| 13 | 768–1023 을 다루는 규칙 **≥1** | | |

⚠️ **감춤은 속성이 아니라 실제 렌더 높이로 확인한다.** 작성자 `display` 선언이
브라우저 기본 `[hidden]{display:none}` 을 이긴다(`FAILURES.md#hidden-attr`).

---

## C. 쓰는 내내 지키는 것

1. **`manifest.json` 을 읽기 전에 어떤 UI 도 그리지 않는다.** 읽은 증거는 G1·G2 산출물이다 —
   `$status` 값과 자산 대조표를 내지 않았다면 읽지 않은 것으로 본다. 자산 ID 를 추측하지 않는다.
2. **`spec` 값을 임의로 바꾸지 않는다.** ⚠️ 치수는 테마마다 다르다 — `spec` 은 토큰 이름이고
   실제 px 는 **`specByTheme.<테마>`** 에 있다. 이미지에서 눈대중으로 재지 않는다.
3. **자산 spec 의 값이 곧 CSS 변수인 것은 아니다.** `--size-*` 는 `semantic.size` 에 있는 것뿐이다.
   자산에만 있는 항목은 `specByTheme` 에서 값을 읽고 **같은 값을 가진 semantic 토큰**을 찾아 쓴다.
   ⚠️ 없는 변수는 조용히 빈 값이 된다(`FAILURES.md#missing-var`).
4. **색을 헥스로 쓰지 않는다.** `tokens` 필드의 시맨틱 토큰명을 CSS 변수로 쓴다.
5. **글자 크기를 숫자로 쓰지 않는다** — `var(--type-heading1-size)`, 축약형 `font:var(--type-heading1)`.
   **간격도 임의값을 쓰지 않는다** — `var(--space-*)` 에서 고른다.
6. **시간을 숫자로 쓰지 않는다.** §F 의 축약형에서 고른다. 실제 ms 는 토큰이 정답이고
   **이 문서에 적힌 숫자가 아니다**(`FAILURES.md#doc-token-drift`).
7. **`status: "deprecated"` 자산은 쓰지 않는다.** `replacedBy` 를 따라간다.
8. **상태는 자산이 아니다.** `variantOf` 가 있는 항목은 부모 자산의 상태 그림이다.
   규격은 부모와 같고 **토큰만 다르다.**
9. **표시 폭의 `--media-density` 배로 이미지를 요청한다**(`FAILURES.md#blurry-2x`).

---

## D. 사이트 · pack · core

**자산이 먼저가 아니라 사이트가 먼저다.** 고객사 건은 그 사이트의 정의 하나로 시작하고,
테마는 그 안의 **값 블록**일 뿐이다.

| 층 | 무엇을 정하나 | 바뀌는 단위 |
|---|---|---|
| `core` | 브레이크포인트 · 접기 어휘 · 모션 · 미디어 비율 · 한글 조판 · 아이콘 · **활자 role** · 접근성 바닥 · 뼈대 순서 | **바뀌지 않는다** |
| `pack` | **자산 목록 자체** · 그룹 어휘 · 업종 치수 토큰 · 핵심 업무 흐름 | 업종마다 |
| `site`(테마) | 색 · 치수 · 라운드 · 활자 크기 · 폰트 · 컨테이너 폭 | 고객사마다 |

1. **core 에 업종 어휘를 넣지 않는다.** core 는 "공통으로 쓰이는 것"이 아니라
   **"업종이 바뀌어도 참인 것"** 이다(`FAILURES.md#core-polluted`).
2. **자산이 필요한 층은 `pack` 이다.** 예약은 그 호텔의 것이 아니라 **호텔 업종의 것**이다.
3. **핵심 흐름(`coreFlow`)은 필수다.** 비워 두고 시작하지 않는다.
4. **`site` 는 자로 잴 수 있는 것만 담는다.** 못 재면 site 가 아니다.
   정렬·풀블리드·겹침 같은 **구조는 토큰으로 오지 않는다** — `rules` 에 문장으로 적는다.

### 테마

| 테마 | 무엇 |
|---|---|
| `eluo` | ELUO 브랜드. Navy `#000080`. 자사 제안·내부 산출물의 기본값 |
| `atlas` · `ember` · `harbor` · `tideland` · `cobalt` · `noir` · `linen` | 고객사 테마(전부 가명). 색·치수는 `manifest.json` 의 `foundationByTheme` 를 본다 |

> `ember` 는 이름과 색이 다르다(딥그린). 브랜드색은 언제나 `--color-brand-primary` 에서 읽는다.
> `tideland` 는 **색부터 추정이라 대외 제출물에 쓰지 않는다.**

**`$status`** — `measured`(색·치수 모두 실측) · `partial`(색은 실측, 치수는 일부) ·
`estimated`(**대외 제출 금지**). 없으면 빌드가 실패한다. 축별 현황은 `index/MEASURE.md`.

**값 뜨는 법** — 공개 웹사이트의 computed style 을 읽는다.
**루트 폰트 크기를 두 배로 올려 다시 재고, 움직인 값만 rem 이다**(`FAILURES.md#rem-trap`).
실측값이 접근성 기준에 걸리면 기준 없는 자리로 내리고 파생값을 넣은 뒤 `$derived` 에 적는다
(`FAILURES.md#contrast-measured`).
**무엇을 옮기고 무엇을 안 옮겼는지 `$measured` 에 적는다.**

---

## E. 화면 기준

**틀리지 않게 하는 것(1~12)과 인상이 남게 하는 것(13~17)은 다른 일입니다.**
1~12 만 지킨 화면은 감점이 없지만 기억도 없습니다.

1. **한 화면에 주장은 하나.** `display1` 도 `cta-band-lg` 도 페이지에 하나뿐이다.
2. **훑는 순서를 만든다.** 제목 → 리드 → 내용 → 액션 하나. **제목만 이어 읽어도** 요지가 나와야 한다.
3. **아이브로우는 페이지에 최대 두 개.**
4. **세로 여백은 이름 있는 셋뿐.** `section.padY`(기본) · `padYTight`(같은 주제가 이어질 때) ·
   `padYLoose`(장이 바뀔 때, **페이지에 최대 2회**). 섹션 안 덩어리 사이는 `block.gap`.
   **이 셋 밖의 값을 만들지 않는다** — 눈대중으로 조절하는 것이 금지이지 리듬을 주는 것이 금지가 아니다.
   **이 조항은 여백에만 적용된다.** 폭·그리드 배분·정렬·이미지 크기는 **오히려 달라야 한다**(13번).
   ⚠️ `FAILURES.md#rhythm-overread`
5. **경계는 셋 중 하나만.** 테두리·배경·그림자를 한 요소에 동시에 쓰지 않는다.
6. **실제 콘텐츠로 짠다.** 제목이 두 줄인 경우, 목록이 1개와 12개인 경우를 다 확인한다.
7. **빈 상태를 함께 낸다.** 빈 화면은 사과문이 아니라 다음 행동이다.
8. **긴 값은 자르지 말고 흘린다.** 한글 제목에 `nowrap`+말줄임을 쓰지 않는다. 두 줄까지 허용.
   **spec 에 없는 `ch` 숫자를 만들지 않는다**(`FAILURES.md#title-clipped`).
9. **hover 로만 알 수 있는 정보를 만들지 않는다.** 클릭할 수 없는 요소에 hover 를 걸지 않는다.
   나란한 같은 종류 카드는 **전부 링크이거나 전부 아니어야 한다**(`FAILURES.md#hover-on-article`).
10. **포커스를 지우지 않는다.** `outline:none` 금지. sticky 헤더가 있으면 앵커에 `scroll-margin-top`.
    페이지 맨 앞에 본문 바로가기.
11. **사진 위 글자는 스크림을 깐다.** 요소 전체에 `opacity` 를 걸지 않는다. 글자는 100%.
12. **효과를 넣기 전에 그게 무엇을 알려주는지 답한다.** 답이 "예뻐서"면 넣지 않는다.

    **12-1. 페이지 유형을 테마와 함께 선언한다** — `document`(기본값) 또는 `media-led`.
    문장과 수치로 파는 것(제안서·리포트·B2B·IR·채용)이 `document`,
    사진과 장소로 파는 것(호텔·공간·리테일·F&B·패션)이 `media-led` 다.
    **선언하지 않으면 `document` 이고, 아래 셋이 잠긴다.** 결과 하단에 어느 유형인지 적는다.

    **12-2. 두 유형 모두에서 쓰지 않는 것** — 글라스(배경 블러) · 장식 도형 · 아이콘 배경 원 ·
    면을 채우는 배경 그라디언트 · 텍스트 그라디언트 · 그림자 두 겹 · `text-shadow`.
    **업종에 따라 달라지지 않는다.** 정보를 하나도 늘리지 않고, 블러·글라스는 대비비를
    계산할 수 없게 만들어 10·11번과 정면으로 부딪힌다.
    **그라디언트는 스크림으로서만 허용한다** — 장식으로는 금지.

    **12-3. `media-led` 에서 열리는 것은 셋뿐이다** — ① 풀블리드(§F) ② 스크림 변형 6종(§F)
    ③ spec 에 `overlapTarget` 이 적힌 겹침(§F). 목록에 없는 효과는 여기서도 열리지 않는다.

    **12-4. `media-led` 는 사진 면적·크기 단계·풀블리드 개수를 수치로 만족시킨다.**
    판정선은 `docs/REFERENCE.md` 「페이지 유형별 판정선」에 있다.
    굵기는 테마가 정한다 — 시안에서 `font-weight` 를 직접 쓰지 않는다.

13. **레이아웃은 네 축으로 변주한다.** 형태만 바꾸는 것으로는 부족하다.
    G4 구성표에 섹션마다 **네 칸을 다 적는다** — 형태 / 폭 / 배분 / 정렬.

    | 축 | 최소 기준 | 고를 수 있는 것 |
    |---|---|---|
    | **형태** | **3종**(섹션 5개↑이면 4종) · 같은 형태 연속 2회까지 · 한 형태 점유율 50% 이하 | 전면 미디어 · 좌우 분할 · 카드 그리드 · 가로 레일 · 줄 목록 · 수치 레일 · 접힌 것 · 띠 |
    | **폭** | **2단계 이상** | `container.narrow`(글) · `container.max`(기본) · `container.wide`(갤러리·표) · 풀블리드(`media-led` 만) |
    | **그리드 배분** | 같은 배분 **연속 2회까지** | 6/6 · 7/5 · 5/7 · 8/4 · 4/8 · 4/4/4 · 3/3/3/3 · 오프셋 |
    | **정렬** | 본문 섹션 제목은 **왼쪽 고정** | 가운데는 KV · 전환 띠 · 인용에만 |

    배분·폭 목록과 태블릿 대응은 `docs/REFERENCE.md` 에 있다.
    ⚠️ **"제목 → 리드 → 그리드"는 하나의 형태다.** 안에 든 내용이 달라도 형태는 같다.
    **폭도 배분도 정렬도 같으면 형태 이름만 다른 같은 화면이다.**
14. **큰 사진이 최소 하나는 있어야 한다.** 본문 안에 콘텐츠 폭 100% 미디어 블록 1개 이상 ·
    가장 큰 사진 ≥ 콘텐츠 폭의 60% · 받은 자산 **사용률 60% 이상** ·
    **사진 표시 폭은 최소 3단계**(전면·절반·썸네일). 쓰지 않기로 한 장수와 이유를 하단에 한 줄로.
15. **활자는 대비로 위계를 만든다.** 크기 **4~6단계** · **최대 ÷ 본문 ≥ 3.0** ·
    인접 단계 중 최소 한 곳은 **1.6배 이상** · 굵기는 **2종까지.**
    새 크기를 만드는 게 아니라 있는 role 을 덜 쓰고 있었던 것이다.
16. **가장 잘 팔리는 컷을 가장 크게 쓴다.** 호텔=객실→조식→수영장→라운지(**건물 외관은 뒤로**) ·
    커머스=착용/사용 컷 · 문화=작품 · 채용=일하는 사람.
    같은 카드 세트에서 한 피사체가 **60% 를 넘지 않는다.**
    ⚠️ **탭 안쪽은 없는 것과 같다**(`FAILURES.md#photo-buried`).
17. **누를 것을 먼저 정한다.** 탭·필터 칩·아코디언 중 최소 1종.
    0종이면 모션 규칙을 다 지켜도 정적으로 나온다(`FAILURES.md#static-screen`).

---

## F. 반응형 · 모션 · 이미지

### 반응형 — max-width 하향식만 쓴다

`sm`=480 · `md`=768 · `lg`=1024. 자산별 접기는 **`assets[].responsiveByTheme.<테마>`** 가 정답이다
(`fill`·`toCard`·`scrollX`·`stack`·`menuNav`). 아래는 **페이지 전체**를 접는 법이다.

**전체 범위표는 `docs/REFERENCE.md` 에 있다** — 컨테이너·거터·격자·카드·섹션 여백·내비·활자·
누를 것·`*-lg` 버튼, 그리고 `media-led` 의 풀블리드·스테이지 높이·겹침까지 한 표에 있다.

**절대 일어나지 않아야 할 것** — 375px 가로 스크롤 · 뷰포트보다 큰 고정 px 폭 · 본문 16px 미만 ·
`min-width` 미디어쿼리 · **768–1023 을 손대지 않고 넘어가는 것**(`FAILURES.md#tablet-abandoned`).

**44 보다 작은 것**(`pagination.itemSize` 40 · `toggle.size` 40 · `chip.height` 36 · `control.sm.height` 36)은
**spec 을 그대로 두고 `::after` 로 히트 영역만 넓힌다.**

### 모션

1. **모션은 사람의 행동에 대한 대답이다.** hover · focus · press · 상태를 바꾸는 클릭에만 건다.
2. **축약형 여섯에서 고른다** — 반응은 `var(--motion-fast|base|slow|exit)`,
   등장은 `var(--motion-entrance)`, 사진 확대는 `var(--motion-zoom)`.
   **반응은 280ms 가 천장이다.** 등장만 그보다 길다. 둘을 섞지 않는다.
3. **움직일 수 있는 속성은 다섯뿐** — `opacity` · `transform` · `background-color` · `border-color` · `color`.
   `transition` 은 `:hover` 가 아니라 **기본 상태에** 건다.
4. **사진 hover 확대** — `var(--motion-scale-hover)` + `var(--motion-zoom)`, 부모에 `overflow:hidden`,
   **사진에만**(카드 전체를 키우지 않는다), **감싼 요소가 링크·버튼일 때만.**
5. **스크롤 진입 등장** — `opacity` + `translateY(var(--motion-distance-reveal))`,
   `var(--motion-entrance)`, `IntersectionObserver` 로 **한 번만**, 섹션 단위,
   나란한 것만 `var(--motion-stagger)` 씩 **최대 4개**, **히어로에는 걸지 않는다.**
   ⚠️ **JS 가 죽어도 내용은 보여야 한다** — 숨기는 클래스를 HTML 에 박지 말고 JS 가 붙인다.
6. **만들지 않는 것** — 페이지 로드 시퀀스 · 패럴랙스 · 카운트업 · 타이핑 · 무한 마퀴 · 커서 추적.
7. **자동 재생은 셋을 다 갖춰야 한다** — 멈춤 버튼(`mouseenter` 는 터치에서 동작하지 않아
   대신하지 못한다) · `prefers-reduced-motion` 이면 시작 안 함 · `document.hidden` 이면 정지.
   갖춰졌을 때도 **화면당 하나**까지(`FAILURES.md#autoplay-mouseenter`).
8. **`prefers-reduced-motion` 은 토큰 CSS 가 처리한다.** 시안에 그 `@media` 를 쓰지 않는다.

### 이미지

1. **비율은 `var(--media-ratio-*)` 넷 중 하나.** 같은 역할의 이미지는 같은 비율을 쓴다.
   **미디어의 모서리는 감싼 카드와 같은 값이다** — 미디어 전용 라운드 토큰을 두지 않는다.
2. **표시 폭의 `--media-density` 배로 요청한다**(`FAILURES.md#blurry-2x`). 가능하면 `srcset`.
   **자리를 먼저 잡는다** — `width`·`height` 속성 또는 부모에 `aspect-ratio` + `object-fit:cover`.
3. **첫 화면 이미지는 `fetchpriority="high"`**, 나머지는 `loading="lazy" decoding="async"`.
   **로딩 전 배경은 `surface.sunken`** — 흰 깜빡임을 만들지 않는다.
4. **한 페이지에서 톤을 섞지 않는다.** 조도·시간대·색온도를 검색어에 함께 넣어 한 벌로 고른다.
   **`object-position`** 기본은 `center`, 인물·건물은 위쪽. 얼굴·간판이 잘리는 크롭을 두지 않는다.
5. **`alt`** — 장식이면 빈 문자열, 정보면 무엇이 보이는지. "이미지"·"사진" 같은 말을 쓰지 않는다.
   **범용 이미지는 시안임을 밝힌다** — 출처와 "납품 시 교체 필요"를 하단에 한 줄로.

### 사진 위 글자 — 스크림

**요소 전체에 `opacity` 를 걸지 않는다.** 글자는 100% 로 두고 스크림만 깐다.
**글자를 어디에 놓느냐로 고른다.** 여섯뿐이고 새로 만들지 않는다.

**여섯 종류의 쓰임은 `docs/REFERENCE.md` 에 있다.**

**깔았다고 읽히는 것은 아니다.** 글자 상자를 8×8 샘플해 95퍼센타일 휘도 `L95` 를 재고,
스크림 알파 `a` 에 대해 **`L' = L95·(1−a) + 0.0045·a ≤ 0.179`** 를 확인한다(흰 글자 4.5:1).
미달이면 순서대로 — ① 스크림 종류 교체 ② `object-position` 조정 ③ **사진 교체** ④ `plate`.
**`text-shadow` 로 때우지 않는다** — 대비비 계산에 안 잡혀 검사기를 통과하면서 실제로는 안 읽힌다.
⚠️ `even` 은 설경·백사장·역광에서 거의 항상 미달한다. 밝은 KV 에 가운데 정렬 제목을 얹기로 했으면
**사진을 고르는 단계에서 계산한다.**

---

## G. 참조표는 따로 있다

**자산 카탈로그**(페이지를 이루는 덩어리 · 뼈대 순서 · 아이콘)와
**구조 · 경로 · 빌드**(파일 구조 · 자산을 쓰는 3가지 경로 · 파이프라인 · 일관성 QA)는
`docs/REFERENCE.md` 에 있습니다. 규칙이 아니라 조회용이라 본문에서 뺐습니다.

기억할 것 둘만 여기 둡니다.

- **히어로는 둘 중 하나만 쓴다.** 수치를 앞세우면 `hero-split-lg`, 사진을 앞세우면 `hero-media-lg`.
  둘을 같이 놓지 않고, `hero-split-lg` 밑에 사진을 덧붙이지도 않는다.
- **뼈대 순서** `header-gnb-lg → 히어로 → 본문 → cta-band-lg → footer-lg`.
  히어로는 GNB 바로 다음이고, **검색·필터 같은 도구는 히어로 아래**다.
  브리프의 섹션 나열 순서를 배치 순서로 읽지 않는다(`FAILURES.md#brief-order`).

---

## I. 하지 않을 것

- manifest 에 없는 색·폰트·라운드를 "보기 좋아서" 도입
- 생성물(`manifest.json` · `docs/` · `index/` · PNG)을 직접 편집
- 사람의 승인 없이 `tokens/` 나 `recipes/` 수정
- **저장소는 public 이다.** 고객사 로고·실제 콘텐츠·미공개 화면을 넣지 않는다.
  **브랜드 전용 서체 이름도 적지 않는다** — 그것 하나로 회사가 특정된다(`FAILURES.md#font-leak`).
  **데모 데이터도 공개물이다** — 실제 프로젝트명·사람 이름을 쓰지 않는다(`FAILURES.md#demo-data-leak`).

---

## 규칙서 자체의 규칙

- **관문 7개 · 점검 항목 25개가 상한이다.** 새 규칙은 자리를 새로 만드는 게 아니라
  기존 항목을 교체하거나 점검 항목 한 줄로 환산한다. 상한에 걸리면 무엇을 뺄지 함께 정한다.
- **상한은 두 개다.** ① 반드시 읽는 §A + §B 가 **180줄** ② 파일 전체가 **400줄.**
  넘으면 조회용 표를 `docs/REFERENCE.md` 로 내보내고 링크만 둔다.
  (처음에 "전체 300줄" 하나로 잡았는데, 그건 목적을 잘못 옮긴 지표였다.
  목적은 짧은 파일이 아니라 **앞 두 절만 읽어도 큰 실패가 안 나는 것**이라,
  ①을 주 지표로 두고 ②를 보조로 뒀다. 숫자를 늘릴 때는 무엇을 대신 뺐는지 함께 적는다.)
- **⚠️ 사례는 `FAILURES.md` 에 쓴다.** 본문에는 한 줄과 앵커만 둔다.
